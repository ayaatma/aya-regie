# Memory Index

**Working on the UI? Read [Admin UI](feature_admin_ui.md) and [Engine API](project_engine_api.md).
Neither the full brief nor the engine source is needed for that.**

- [Git commit and push](feedback_git_commit_push.md): **this project only, since 2026-09-14**, Claude commits and pushes to origin main unprompted once a unit of work is coherent and green; never amend, force-push or rewrite history.
- [Open source](project_open_source.md): **2026-09-14, the tool is AyaRégie, public and MIT on github.com/ayaatma/aya-regie.** Nothing secret, no hosting login, no real personal data in any tracked file, memory included.
- [Project Brief](project_brief.md): the why behind everything, written as dated layers. Loto Tekno, 13 March 2027, running 12h to 06h, form out mid-October 2026, about 600 person-hours over about 15 poles. React + Supabase on an OVH subdomain. Availability is the refused slot and nothing else since 2026-09-08; the loto / soirée split is a scored preference, see [Preference refactor](feature_preference_refactor.md). The solver must never return infeasible. **State 2026-09-08: the headless engine is complete and verified, and so is the admin UI: grid, grouped proposals, dashboard, recruitment, Réglages (including the form's time slots), import from a Google Sheet, a printable page of every schedule, keyboard navigation, and a read-only phone night view. The importer now binds against the real form's 40 columns. Plan history, named checkpoints, restoring a version, deleting a plan and an activity journal are built, and the buddy answers of the real form are read properly. The volunteer view is built: a code, no account, one person's own shifts. The generator writes the real 40-column export, decoys included. The access codes leave the tool as a Brevo contact file. Since 2026-09-09 the régisseurs log in with a password rather than a link at every login, see [Auth](feature_auth.md). **Deployed and working at https://planning.ayaatma.fr since 2026-09-08**, one command (npm run deploy in tools). 170 engine + 218 app tests. The database is level with the repository since 2026-09-08 20:49: the one migration the ledger wrongly called applied was found and run, data intact. Short display names were reworked on 2026-09-09 and the form's surname question is read, see [Display names](feature_display_names.md): PLAN_FORMAT 3, migration 10 applied and verified on 2026-09-09 at 23:20, deploy unchecked. The pole-leader access is built and live since 2026-09-09, see [Leader access](feature_leader_access.md): a migration that must always ship with its deploy. PLAN_FORMAT is 4 since 2026-09-10, see [Free-text answers](feature_free_text_answers.md). Migrations no longer go through the SQL editor, npm run migrate in tools executes them over a real Postgres connection, one transaction per file with its ledger row written inside it, pinned to Supabase own root CA, and verifies the shape afterwards; npm run db-check asks the same question on its own.**
- [Event abstraction](feature_event_abstraction.md): **2026-09-13, built, green, migration 16 APPLIED (the migrator listed it as « déjà appliquée » later that day), PLAN_FORMAT 8 then 9 with [Artists](feature_artists.md).** The loto left the code: `halfPreference` and the two rules `eveningStartsAt` / `afternoonOverflowUntil` became `Plan.preferenceSlots` (Loto, Concerts, each with its own « débordement accepté ») and `Volunteer.preferredSlotId`; TWO lists and two types because the refusable tranches must tile the event and the preferable ones may overlap (two tests proved it). Also: `Plan.address`, a one-week cap, the montage ends where the event starts and the démontage starts where it ends (`alignPhases`, run on every event or phase edit and on load), two days each by default, one bénévole edge per phase, the catering meta reworded, and the day written on the exploit's axis with a doubled midnight line. Read it before touching a preference, a phase edge or the rulers.
- [Artists](feature_artists.md): **2026-09-13, « Artistes V2 », built, green, MIGRATION 17 APPLIED AND DEPLOYED on 2026-09-13, PLAN_FORMAT 9.** The line-up is a fiche per act on its own tab « Artistes » (the Programmation block left Réglages): size, changement de plateau, balances stored as hours of the event and NEGATIVE the day before (drawn on the montage translated, never stored as a PhaseEvent), défraiement with car trips, rider, members with meals as MealChoice rows of kind 'artiste' (presence rule, no quota), tickets that follow `CateringRules.artistDrinks` until set by hand (null = follow), payment, invitations (names since the billetterie, see [Ticketing](feature_ticketing.md)). **Second round the same day: a member may ALSO be a bénévole or an orga (`linkedKind` / `linkedKey`): one row on the caterer's sheet, the person's own, absorbing the act's presence (never two plates), and tickets settled by `artistDrinksCumulative` (off = the higher status, on = the sum).** Ruler height is inline now, three band colours. No browser check. Read it before touching an artist, the ruler, the meal kinds or NumberField.
- [Tabs and pole filter](feature_tabs_and_pole_filter.md): **2026-09-13, built, green, no schema.** The pool of the three moments has a « Pôle demandé » filter (choix 1 / choix 2 / responsable chips, `poleMatchOf`), the solver's `choice2` weight went 20 → 100 with the measurement, and the ten tabs are seven: Tableau de bord + Recrutement, Import/Export, Historique + Journal (folded), through `StackedScreen`, a shell around the unchanged screens. Read it before touching the tabs, the pool filters or a solver weight.
- [Ticketing](feature_ticketing.md): **2026-09-13, la billetterie, built, green, migration 18 APPLIED, migration 19 (travel costs, door remarks and drink figures) APPLIED 2026-09-13, PLAN_FORMAT 11.** Check `npm run migrate` before editing any migration written earlier in the session: 18 was edited after it had run and had to be rebuilt. One derived list of everybody who gets in without a ticket (statuses, drink and meal tickets from the catering, ticket type and bracelet with defaults from Réglages, only the picks stored), search, status filter, CSV with or without phones, extras typed on the tab. Invitations of an act are NAMES now (`guests` / `extraGuests`), and « effets Artiste » reached the catering (non-cumulative = the artist's figures, not the max). Read it before touching a status, a ticket, a bracelet, a guest or a linked member.
- [Personnes tab](feature_people_tab.md): **2026-09-15, the Billetterie tab is « Personnes »**, with the reserve in a card of its own under the list (left the grids' pool pane the same day): list with Accueil / Contact / Repas columns, a fiche pane per kind of person (reusing `VolunteerFiche` / `OrganiserFiche`), door fields shared in `DoorFields.tsx`, « Ouvrir dans Personnes » from the grids. V2 converts bénévole ↔ orga (`convert.ts`, proposal then one edit), with import safeguards; **PLAN_FORMAT 15: migration 22 applied; MIGRATION 23 (réserve dans la liste d'entrée, a Réglages setting) WRITTEN AND NOT APPLIED, ships with its deploy.** Read it before touching the tab, a fiche, a conversion or the import's removals.
- [Generic event](feature_generic_event.md): **2026-09-14, built, green, MIGRATIONS 20 AND 21 PENDING, PLAN_FORMAT 13.** N pole choices ranked or not (`Volunteer.choices`), volume per event or per day with a day boundary hour (`days.ts`), a remembered import correspondence of columns and answers (`form-mapping.ts`, `FormMappingCard`), neutral new events and no Loto branding, a cost warning on drag (`costsFor`), binômes added or removed by hand. Read it before touching a choice, a volume, the import binding or a buddy.
- [Réglages avancés](feature_advanced_settings.md): **2026-09-13, built, green, MIGRATION 20 WRITTEN AND NOT APPLIED, PLAN_FORMAT 12 (13 since, see Generic event).** Per event, every planning criterion is Bloquant / Poids / Ignoré with a weight (`tools/src/constraints.ts`, `Plan.constraints`, one jsonb column); the « Règles de planning » card is gone, its thresholds sit on their criterion rows; an issue code never changes with the mode, only its tier; `DEFAULT_WEIGHTS` is derived. Read it before touching a rule, a weight, `isLegal` or a solver penalty.
- [Engine API](project_engine_api.md): the engine's public surface, the issue codes, the invariants and the measured timings (validate 4 ms, a full solve 2.4 s). Code the UI against this instead of reading `validate.ts` and `solver.ts`. Reached from the app through the single barrel `app/src/engine.ts`.
- [Admin UI](feature_admin_ui.md): built 2026-09-07 to 2026-09-08 and working. **A refused save keeps the working copy since 2026-09-08; it used to reset the whole state and lose every unsaved edit.** Where the code lives, the forty-three decisions taken, why the form's time slots are data rather than a type and why removing one never rewrites somebody's answer, why the phone gets a different application instead of a squeezed grid, the convergence button (whose first stopping rule was wrong: convergence was a property of the seed, not of the plan), why every hour in Réglages is now one 24 h clock field instead of an offset spinner plus a locale-driven AM/PM control, and why moving a start slides the whole window while moving an end only changes the duration (2026-09-09), why duplicating a sous-pôle copies its créneaux and nobody and why reordering them is exposed on sub-poles only (2026-09-09), and the four measurements not to redo: what the anti-fragmentation weights cost, why proposal dependencies must be directed (symmetric gave one group of 65 out of 78), what the buddy weight costs and why it stops at 5000, and that repeated re-solves do converge. **Since 2026-09-11 the three moments share ONE right-hand panel in TWO panes**, `PoolPanel` (Disponibles / Réserve / À relire, « À zéro » deleted as a tab and kept as a mark on the row) and `InfoPanel` (« Info sélection », one `Selection` type for a bénévole, an orga, a créneau, a phase box or an événement); `SidePanel` and `PhasePool` are gone, clicking anything selects and never decides, and the montage's panel had no CSS at all before this. **Since 2026-09-12 the orgas are in « Disponibles » in the three moments, the phase day filter lives on the toolbar and drives both panes, and a bénévole's name is written once. The phantom `--paper` token is gone (it silently broke the moment selector and three backgrounds), the zoom is a logarithmic slider with an « Ajuster » that fits the grid to the screen, a long day is 💪 / 💪💪 instead of two oranges, and dropping a box on any grid's background unplaces the person.** **2026-09-13: deux responsables au même moment sur un pôle ont chacun leur ligne (`OrganiserBand` empile avec `packRows`, clé sur la personne, hauteur posée en ligne); l'onglet Historique était dans une `.screen` réservant 330 px à un volet absent et dans une `.card` plafonnée à 520 px, sans rien qui défile. Les classes de coquille large sont communes: `.screen-body`, `.screen-card`, `.screen-card-note`, `.screen-scroll`.**
- [Supabase](feature_supabase.md): the persistence layer, started 2026-09-08. **Read "The ledger lied" before trusting schema_migration about anything.** Why db/schema.sql was rewritten whole rather than patched (it predated the engine model), the eight decisions behind it (natural keys, decimal hours as the only time truth, whole-plan saves, four RPCs under RLS, write_plan_body hidden in a private schema, no unique constraint on assignment because the engine tolerates a doublon), the version history, the named checkpoints, the deletion of a plan and the activity journal added 2026-09-08 with their decisions, the plan-format guard against a stale tab, the migration ledger, and the three commands that check the database without one: npm run sql-check, npm run schema-check and npm run anon-check.
- [Auth](feature_auth.md): the régisseur login, reworked 2026-09-09. The password is now the everyday door and the magic link opens an account the first time or reopens it when the password is gone; each person chooses their own password from « Mon compte », so none is ever distributed and no service_role key enters the repository. Read this, not the auth paragraphs in [Supabase](feature_supabase.md), which are marked superseded.
- [Leader access](feature_leader_access.md): responsables de pôle as people rather than one row per pole, **built, deployed and verified against the live base on 2026-09-09.** PLAN_FORMAT is 2, `Leader` and `LeaderRole` replaced `PoleLeader`, their own form imports, a 14-character code opens the whole planning read-only on their pole. Read it before touching leaders anywhere, and read the identity rule before touching any of its three copies.
- [Display names](feature_display_names.md): the short label on a grid box, a volunteer's schedule and the public planning, reworked 2026-09-09. The surname grows only as far as it takes to tell two people apart, the form's Surnom answer replaces the first name, the rule lives once in tools/src/display.ts and Postgres reads volunteer.display_name rather than reimplementing it. Built and green, migration 10 not yet applied.
- [Free-text answers](feature_free_text_answers.md): 2026-09-10, the form stopped asking the time constraint as a radio button and the pole choices gained an « Autre » box. The importer interprets the prose and flags what it is unsure of, the raw sentence is kept forever beside the reading, the régisseur corrects a fiche by hand from the panel, and a re-import never undoes a correction. `refusedSlot` became `refusedSlotIds`. The real export has 44 columns and kept the old closed question beside the new free-text one, so both are read and merged. The Google Sheet link is remembered on the plan since 2026-09-10, so the import screen offers « Rafraîchir ». PLAN_FORMAT 4. Migration 11 is applied; migration 12 (event.sheet_url) is written and dry-run but NOT applied, and it exists because a migration that has already run must never be edited.
- [Montage / Démontage and orgas](feature_montage_demontage.md): **2026-09-10, built, green, migration 13 applied and deployed.** **A declaration is a placement since 2026-09-11**: the boxes are written from the form answers and a box that contradicts one turns red, there is no second « déclaré » state. The grid is a real timeline (one bar per person, one row per person, nights not drawn), the événements share one lane with places to fill, orgas carry a 🤩 on all three grids, and `npm run form-csv` writes a test export for the people a plan already holds. Two kinds of person (bénévoles and orgas, a responsable being an orga holding a pole), and two grids beside the exploit, each with its own origin, its own poles, its unworked nights and its punctual événements. `Leader` is `Organiser` everywhere, PLAN_FORMAT is 5, and an orga can hold a place in an exploit créneau through `PlanIndex.headcountOf`. **Sixth round 2026-09-11: the three reasons trimming a box barely worked (a key that remounted the grip mid-drag, a commit reading state its listener predated, and `hoursAt` answering null in the night), and the TIME ZONE BUG that turned every imported orga red «jusqu'à 1h» (a naked wall clock read as UTC by Postgres, so a phase start came back an hour late; the live data needs the start hour re-entered once).** **Seventh round 2026-09-12: a day filter on the phase grid (the AXIS narrows, not the grid, and the picker drives the pool list too), the orgas in the exploit’s « Disponibles » draggable onto a créneau, and a drop on a pôle’s frise that makes an orga responsable for 2 h with both edges pullable. `DragPayload` is now `{kind, personKey, personName}` and `DRAG_MIME_ORGA` rides beside `DRAG_MIME`. No schema, no plan format. Huitième round le même jour: le filtre jour était rendu mais introuvable (il porte le mot « Jour » sur la barre, et un contrôle que personne ne voit n'existe pas), le volet ne scrolle plus en travers (`.rules-grid` sur une colonne dans `.panel`), une case sélectionnée entoure les autres cases de la même personne (`selectedPerson`, qui compare le genre de personne autant que la clé), le volet propose un orga sur un créneau même complet, et le bandeau responsable se glisse en entier (corps = déplacer, bord = étirer, clic court = fiche). **Neuvième round 2026-09-12, LE SEUL AVEC UNE MIGRATION: `Pole.leaderSupportOnly` (« le responsable reste en support, sans créneau sur ce pôle »), signalé par `PlanIndex.supportOnlyBreaches` et jamais refusé, MIGRATION 14 APPLIQUÉE le 2026-09-12; PLAN_FORMAT est passé à 7 depuis, voir [Catering](feature_catering.md).** Avec: le token `--paper` n'existait pas et cassait quatre règles dont le sélecteur de moment (clair sur clair), le zoom est un slider logarithmique avec « Ajuster » et la grille se cale sur la largeur quand le filtre jour change, la journée de 6 h / 8 h est 💪 / 💪💪 au lieu de deux oranges, déposer une case sur le fond de n'importe quelle grille la retire, et un échange de deux cases n'affiche plus « placement interdit ».** **Onzième round le même jour, trois retours et deux règles CSS: `.box.is-mini` déclarait `height: 22px` sans toucher au `flex: 0 0 24px` hérité de `.box`, or dans une colonne flex c'est la base qui fait la hauteur (donc `2 × n` pixels manquants, mangés par `overflow: hidden`); et `.box.is-orga-box` posait fond, bordure et couleur sans condition en arrivant APRÈS `.box.is-illegal` à spécificité égale, donc une case d'orga en faute ne rougissait qu'au survol, sur les trois grilles. Les couleurs passent sous `:not(.is-illegal):not(.is-picked)`, la marque reste, l'italique des noms est retiré. `EVENT_GEOMETRY` est exporté et `phaseGeometry.test.tsx` RELIT `styles.css`: deux erreurs de géométrie avaient survécu à une suite verte parce que chaque test mesurait la même arithmétique que le composant.** **Douzième round 2026-09-13: les flèches et Suppr sur les deux phases (`phaseNav.ts`, un second fichier et pas un paramètre de `gridNav.ts` parce qu'une phase n'est pas un damier: « bas » est la case la plus proche dans le temps sur la ligne d'en dessous, et le curseur EST la sélection plutôt qu'un second état).** Read it before touching the phases, `Organiser`, trimming, the exploit’s drag and drop, or anything that reads a créneau’s effectif.
- [Catering](feature_catering.md): les repas et les tickets boisson, **built 2026-09-12, green, MIGRATION 15 WRITTEN AND NOT APPLIED, PLAN_FORMAT 7.** A meal service is a REAL DATE AND A REAL HOUR rather than an offset, so midi of the event day is one plate whether the montage or the exploit brought somebody there. Two rules that are not the same rule: on the exploit a meal is EARNED by the hours worked (a step function the régisseur types in Réglages, plus an orga floor that lifts and never caps) and the quota is spent on the services nearest their hours; on the two phases a meal is EATEN by whoever is placed across the service. ONLY THE RÉGISSEUR'S DISAGREEMENTS ARE STORED (`MealChoice`), so a moved créneau or a changed tier never freezes yesterday's headcount. `Volunteer.diet` and `.allergies` read at last from form columns 11 and 12, allergy matcher before diet matcher. `PersonKind` moved to `model.ts`. New tab Catering, new Réglages card, CSV for the traiteur. **Deuxième tour le même jour, huit retours, sans schéma ni format: un bénévole n'est plus placé depuis sa réponse au formulaire (`declaredPlacements` ne répond que pour les orgas, et `setVolunteerPhase` n'appelle plus `syncPerson`, qui aurait EFFACÉ les cases posées à la main), les repas d'une phase lisent les cases et, à défaut de case, la déclaration; une case d'événement rougit enfin et `.phase-bar.is-illegal` prend le fond rouge de l'exploit; le bloc d'un événement était trop court de trois pixels et `overflow: hidden` avalait la dernière case; la carte Orgas n'avait aucun padding parce que sa classe `.setup-shifts` ne correspondait à aucune règle du tout; chaque section de Réglages arrive repliée (`SetupSection`, contenu caché et non démonté, à cause des brouillons en cours de saisie); l'écran Catering avait la mauvaise coquille, ce qui expliquait à la fois sa barre de défilement interne et son bas de page inaccessible.** Read it before touching a plate count, a tier, the diet on any fiche, or anything that places somebody on a phase.
- [Preference refactor](feature_preference_refactor.md): 2026-09-08, engine to database. The loto / soirée answer stopped being a hard rule and became a scored preference (boundary 20h, tolerated overflow to 22h, 1500 an hour beyond), pole refusals became a list, and softening the rule took `balanced` from 85/91 créneaux to **91/91 with nothing short** on the same registrations. Read this before touching `availability.ts`, the solver's weights, or either direction of the SQL round trip.
- [Form and import](feature_form_import.md): the real Google Form read on 2026-09-08. Its exact option labels (levels are Débutant/Intermédiaire/Habitué, never expert; the nine pole labels carry emoji), how to re-extract it from the preview page, the four places it did not match the engine and the decisions that closed them, and what the real 40-column export does to the column binding: three decoy questions that each ate a real column, and volume answers that state "2 h de plus" rather than a total. All four decisions are implemented, see [Preference refactor](feature_preference_refactor.md). Since 2026-09-08 the binôme answer is read as one person rather than as a comma-separated list, and resolved by mail address first.
- [Working style](feedback_working_style.md): technical depth without basics, one recommendation with its reason, shaky diagnoses flagged. The personal profile lives in the machine-local memory, not in this public repo.
- [Visual check with headless Chrome](feedback_visual_check_with_headless_chrome.md): `npm run shots` in app/ drives the installed Chrome headless through every tab on the fixtures, both colour schemes, PNGs in app/shots/, on the `balanced+phases` fixture (`withPhases` in tools/src/plan-fixtures.ts: phases, orgas, catering, a full act, all on). Run it after any screen or CSS change and READ the shots; the extension is not needed. Standing rule since 2026-09-13.
- [Migration state hook](feedback_migration_state_hook.md): the developer applies migrations from VS Code silently; a UserPromptSubmit hook injects the last known state (applied / pending / applied-but-edited) from db/.migration-state.json, which every migrate run writes. Check it before saying a migration is pending or editing one; refresh with `npm run migrate` (dry).
- [Generic event thinking](feedback_generic_event_thinking.md): standing rule since 2026-09-13: the tool serves any event; a Loto Tekno example is never a spec, and every event-specific figure becomes a per-event setting with that figure as its default.
- [Session Hygiene](feedback_session_hygiene.md): tell the user unprompted when a conversation has hit a natural cut point and a fresh session would cost less; standing rule, all projects.
- [Proactive Progress Memory](feedback_proactive_progress_memory.md): update project memory files unprompted after real progress, so a fresh session can recover full context after an unplanned loss.
- [No Em Dash](feedback_no_em_dash.md): never use the em dash in anything a user could see; rephrase, do not substitute a hyphen.
- [Gender-Neutral French](feedback_gender_neutral_french.md): user-facing French naming a person must avoid gendered pronouns and participle agreement; roughly half the volunteers are women.
- [Verify Root Cause Before Fixing](feedback_verify_root_cause_before_fixing.md): do not ship a fix for a plausible timing/race/cache theory without isolating reproduction; flag unconfirmed diagnoses as unconfirmed.
- [Don't Reinterpret a Reported Symptom](feedback_dont_reinterpret_reported_symptom.md): when a search for the exact thing the user named comes up empty, ask, do not substitute the nearest thing found nearby.
- [Report migration and deploy](feedback_report_migration_and_deploy.md): standing rule since 2026-09-15, end every piece of work by saying whether a migration and/or a deploy is needed, and in which order.
